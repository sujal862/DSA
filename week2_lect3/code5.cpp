
    int x;
    cout<<"enter a positive integer: ";
    cin>>x;
    if(x%5==0 && x%3==0)   //or if(x%15==0 )
    cout<<"num is divisible by 5 & 3";
    else 
    cout<<"num is not divisible by 5 and 3";
}